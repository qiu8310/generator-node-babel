language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-node-babel' ]; then cd .. && eval "mv $currentfolder generator-node-babel" && cd generator-node-babel; fi
